# laft_g

Utilidades locales para el proyecto `laft_g`.

## Stack
- **Base de Datos**: Postgres 12 (con soporte partman/uuid).
- **Aplicación**: PHP 7.x + Apache.

Instrucciones de despliegue en la carpeta docker/.
