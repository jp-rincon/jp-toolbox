# tobo3_g

Utilidades locales para el proyecto `tobo3_g`.

## Stack
- **Base de Datos**: Postgres 13 (teikadev/postgres13-partman).
- **Aplicación**: PHP 5.3 + Apache + PgSQL.

Instrucciones de despliegue en la carpeta `docker/`.
